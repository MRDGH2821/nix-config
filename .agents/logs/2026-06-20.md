# AI Work Log - 2026-06-20

## 2026-06-20T14:34:35+1000

### Prompt

> help me configure forgejo runner in my nixos config

### Model

Composer via Cursor

### Actions

- Inspected existing `forgejo.nix` and partial `forgejo-runner.nix` module
- Ran `just check` — found missing `name`, `token`/`tokenFile`, and `pkg` typo
- Read nixpkgs `gitea-actions-runner` module for required options and podman integration

## 2026-06-20T14:50:00+1000

### Prompt

> i have configured a token in sops

### Model

Composer via Cursor

### Actions

- Verified user-added `nix/hosts/home-lab/secrets/forgejo-runner.yaml` with `forgejo.default` token key
- Confirmed `sops.nix` maps `forgejo-runner-default` secret to that file/key and restarts `gitea-runner-default.service`
- Confirmed `forgejo-runner.nix` wraps the token in a `TOKEN=` sops template for systemd
- Ran `just check` — all checks passed

### Outcome

✅ Forgejo runner config is complete and validates; ready to deploy

## 2026-06-20T15:10:00+1000

### Prompt

> the build failed / deployment logs show it is not able to find the token

### Model

Composer via Cursor

### Actions

- Traced token wiring: sops template + `tokenFile` rendered to `/run/secrets/rendered/forgejo-runner`
- Renamed secret to `forgejoRunnerToken` (no hyphens) in `sops.nix`
- Switched runner to `token = config.sops.placeholder.forgejoRunnerToken` so sops-nix injects `TOKEN` directly into the systemd unit
- Removed intermediate sops template layer

### Outcome

⚠️ Fix applied locally — redeploy with `just home-lab` to verify runner registration

## 2026-06-20T17:29:19+1000

### Prompt

> configure KDE desktop environment in home-lab config / search online to see how the syntax is, and then wire it up here

### Model

Composer via Cursor

### Actions

- Searched NixOS wiki for current Plasma 6 + Plasma Login Manager syntax on nixos-unstable
- Created `nix/hosts/home-lab/modules/kde.nix` — enables Plasma 6, plasma-login-manager, and xserver
- Added `bose-game` to `audio`, `render`, and `video` groups; enabled Bluetooth for Plasma integration
- Ran `just check` — all checks passed

### Outcome

✅ KDE Plasma 6 desktop wired into home-lab via auto-imported `kde.nix` module

## 2026-06-20T17:35:00+1000

### Prompt

> i want mr-nix to be the user

### Model

Composer via Cursor

### Actions

- Updated `nix/hosts/home-lab/modules/kde.nix` — desktop groups now apply to `mr-nix` (defined in `nix/vars/mr-nix-user.nix`) instead of `bose-game`
- Ran `just check` — all checks passed

### Outcome

✅ KDE desktop user groups wired to `mr-nix`

## 2026-06-20T18:00:00+1000

### Prompt

> remotely login to home-lab and see the DE / research online then implement

### Model

Composer via Cursor

### Actions

- Researched NixOS wiki + Discourse: xrdp + startplasma-x11 is the supported remote-login path for Plasma 6
- Created `nix/hosts/home-lab/modules/kde-remote-desktop.nix` — xrdp, Tailscale-only port 3389, xclip
- Ran `just check` — all checks passed

### Outcome

✅ Remote Plasma login via xrdp over Tailscale-only firewall on port 3389

## 2026-06-20T18:30:00+1000

### Prompt

> implement hyprland instead of KDE / don't implement rdp, remove it

### Model

Composer via Cursor

### Actions

- Removed `kde.nix` and `kde-remote-desktop.nix`
- Added `nix/hosts/home-lab/modules/hyprland.nix` — Hyprland with UWSM, SDDM, mr-nix desktop groups
- Ran `just check` — all checks passed

### Outcome

✅ KDE and RDP removed; Hyprland + SDDM configured for home-lab

## 2026-06-20T19:00:00+1000

### Prompt

> i will use hyprland + waypipe

### Model

Composer via Cursor

### Actions

- Added `nix/hosts/home-lab/modules/waypipe.nix` — OpenSSH socket forwarding, VA-API, waypipe + libva-utils
- Updated `hyprland.nix` — added kitty for default Hyprland config
- Moved waypipe from shared `system-packages.nix` to home-lab waypipe module
- Ran `just check` — all checks passed

### Outcome

✅ Hyprland + waypipe server-side config on home-lab; RDP not used
