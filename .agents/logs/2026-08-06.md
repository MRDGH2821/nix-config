# AI Work Log - 2026-08-06

## 2026-08-06T00:36:00+10:00

### Prompt

> [home-lab activate still fails: sops decrypt 0 groups, nextcloud downgrade on rollback]

### Model

Composer via Cursor

### Actions

- Confirmed host public SSH → age recipient is `age1t6v85…` (matches secrets); admin key `age18mhd…` decrypts locally
- Previous sops.nix fix had not landed; `generateKey=true` left a random `/var/lib/sops-nix/key.txt` not in recipients; sops-nix library SSH import still failing to unlock in practice
- Rewrote sops.nix: activation `sopsAgeHostKey` writes host SSH→age via `pkgs.ssh-to-age` into keyFile _before_ setupSecrets; keyFile-only; `gnupg.sshKeyPaths = mkForce []`
- nextcloud: assert package version ≥ 33.0.7 so eval fails instead of silent downgrade packages

### Outcome

⚠️ Config fix ready; user should redeploy current tip without rolling back

## 2026-08-06T14:51:00+10:00

### Prompt

> home-lab activate still fails setupSecrets (0 successful groups); switch to refactor/blueprint-hm-phase2

### Model

Composer via Cursor

### Actions

- Switched agent root to worktree `refactor/blueprint-hm-phase2`
- Compared failing system gen (`s3ky4b…`) manifest: `ageSshKeyPaths=[]`, keyFile-only; working gens: `ageSshKeyPaths=[/etc/ssh/ssh_host_ed25519_key]`
- Confirmed secrets.yaml recipients include host `age1t6v85…` (not encryption mismatch)
- Discarded staged regression (`generateKey=true`, nextcloud `enable=false`)
- Rewrote `nix/hosts/home-lab/modules/sops.nix`: restore host ed25519 in `age.sshKeyPaths`, `generateKey=false`, remove keyFile-only sopsAgeHostKey convert path; add debug probe activation

### Outcome

⚠️ Fix applied in branch; awaiting user redeploy verification

## 2026-08-06T21:08:00+10:00

### Prompt

> Post-fix activate still fails: host age1t6v85 imported but secrets.yaml still 0 groups; rollback also fails

### Model

Composer via Cursor

### Actions

- Evidence: probe shows host age from ssh pub = age1t6v85; sops-install-secrets imports same; still fails on secrets.yaml
- Admin age key (age18mhd) decrypts secrets.yaml OK; install-secrets -check-mode sopsfile OK with admin key
- Manifest decrypt order implies dotenv secrets likely succeed first; secrets.yaml is the failing file
- `sops updatekeys` reported all files already up to date (does not verify host can open wraps)
- Force-rotated data key: `sops -r -i secrets.yaml` — host wrap hash 667a93… → c02a28…; admin decrypt still OK
- sops.nix: drop age.keyFile leftover random identity; keep sshKeyPaths host ed25519; keep debug probe

### Outcome

⚠️ secrets.yaml re-encrypted for host recipient; redeploy required for verification

## 2026-08-06T21:13:00+10:00

### Prompt

> User confirmed home-lab activate succeeded after secrets.yaml rotate + host age.sshKeyPaths

### Model

Composer via Cursor

### Actions

- Verified post-fix: import age1t6v85, acknowledgment complete, no setupSecrets failure
- Probe matched expectedRecipient; keyfilePub also age1t6v85
- Removed sopsDebugProbe instrumentation from sops.nix; kept permanent fix (sshKeyPaths host ed25519, generateKey=false, no random keyFile)

### Outcome

✅ home-lab sops activation fixed and confirmed by user
