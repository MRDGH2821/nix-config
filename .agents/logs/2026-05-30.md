# AI Work Log - 2026-05-30

## 08:27:50+1000

### Prompt

> Fix honcho-memory PostgreSQL connection URL — use the NixOS-managed PostgreSQL (honcho user/db) instead of hardcoded superuser

### Model

deepseek-v4-flash-free via opencode-zen

### Actions

- Added `pkgs` to lambda parameters in `default.nix`
- Fixed `services.postgresql.extensions` — changed `["pgvector"]` to `[ pkgs.postgresqlPackages.pgvector ]` (was a string, needs package paths)
- Added `authentication = lib.mkOrder 400 ...` with trust rule for podman bridge CIDR (10.89.0.0/16) so containers can reach host PostgreSQL without a password
- Fixed DB_CONNECTION_URI: changed from `postgres:postgres@localhost:5432/postgres` (superuser, wrong db) to `honcho@10.89.0.1:5432/honcho` (dedicated user/db, podman gateway IP)
- Fixed copy-paste bug: deriver's `environmentFiles` referenced `config.sops.templates.linkwarden.path` instead of `config.sops.templates.honcho.path`

### Outcome

✅ Flake validates with `just check`. All changes applied to `nix/system-modules/container-services/honcho-memory/default.nix`
