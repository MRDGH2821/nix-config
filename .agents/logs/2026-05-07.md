# AI Work Log - 2026-05-07

## 2026-05-07T12:00:00+10:00

### Prompt

> do it

### Model

GPT-5.2 via Cursor

### Actions

- Added `HERMES_DASHBOARD_URL` beside `HERMES_API_URL` in `nix/system-modules/container-services/hermes-workspace/default.nix` so the Podman workspace hits dashboard on `host.containers.internal:9119`.
- Mirrored the same env pair in `nix/system-modules/container-services/hermes-workspace/docker-compose.yml` for compose2nix regeneration parity.
- Extended `nix/hosts/home-lab/secrets/README.md` with Hermes Workspace secret notes (`HERMES_API_TOKEN` vs gateway `API_SERVER_KEY`, restart units) and corrected Hermes Agent restart unit wording.
- Ran `just check` — all flake checks passed.

### Outcome

✅ Workspace module and docs updated; `restartUnits` for workspace secrets was already present in `sops.nix`. User must still align `hermes-workspace.env` SOPS content: set `HERMES_API_TOKEN` (same value as `API_SERVER_KEY` in `hermes.env`), not `API_SERVER_KEY` alone.

---

## 2026-05-07T09:13:46+1000

### Prompt

> just create a sample systemd service, I will figure out the rest

### Model

GPT-5.2 via Cursor

### Actions

- Modified `nix/system-modules/services/hermes-agent.nix` — added `systemd.services.hermes-dashboard-sample` with `ExecStart` pointing at `${lib.getExe config.services.hermes-agent.package} dashboard`, no boot `wantedBy` (manual enable/start); wired `lib` into module arguments.
- Ran `just check`.

### Outcome

✅ Sample unit added; flake check passed.

---

## 2026-05-07T09:20:00+10:00

### Prompt

> just create a sample systemd service, I will figure out the rest

_(follow-up: user edits introduced invalid `config.sops.secrets.hermes` and `ExecStart` using `pkgs.hermes-agent`)_

### Model

GPT-5.2 via Cursor

### Actions

- Updated `nix/system-modules/services/hermes-agent.nix` — `hermes-dashboard` sample uses `hermes-env` via `EnvironmentFile`, `lib.getExe` on `config.services.hermes-agent.package`, `Type = "simple"`, `wantedBy = lib.mkForce []` until boot enrollment is intentional.

### Outcome

✅ Sample unit valid Nix/OS again; awaiting `just check` if run.

---

## 2026-05-07T09:31:05+1000

### Prompt

> inside the container, host.containers.internal is not working

### Model

GPT-5.2 via Cursor

### Actions

- Modified `nix/system-modules/container-services/hermes-workspace/default.nix` — added Podman `--add-host=host.containers.internal:host-gateway` in `extraOptions`.
- Modified `nix/system-modules/container-services/hermes-workspace/docker-compose.yml` — `extra_hosts` mirror for compose2nix regeneration parity.
- Ran `just check` — passed.

### Outcome

✅ Host alias wired for Podman/Linux; redeploy/recreate container to pick up `--add-host`.

---

## 2026-05-07T09:50:19+1000

### Prompt

> use host loopback as a fix

### Model

GPT-5.2 via Cursor

### Actions

- Switched Hermes workspace to **`--network=host`**: Podman/container uses host networking so `127.0.0.1` reaches gateway/dashboard on the host.
- Updated `default.nix` — `HERMES_*` URLs → `http://127.0.0.1:…`, `PORT=3100`; fixed trailing brace syntax error.
- Updated `docker-compose.yml` — `network_mode: host`; removed bridge `extra_hosts`.
- Rewrote `podman-compose.nix` — dropped `hermes-workspace_default` network wiring; removed port publish (host net); `README` Hermes Workspace paragraph updated.

### Outcome

✅ `just check` passed; redeploy/recreate workspace container after rebuild.
