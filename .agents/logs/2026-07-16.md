# AI Work Log - 2026-07-16

## 21:15:17+10:00

### Prompt

> Help me setup gitlab runner

### Model

Claude Sonnet 5 via Zed

### Actions

- Investigated existing partially-started, untracked files: `nix/system-modules/services/gitlab-runner.nix` (enabled `services.gitlab-runner` but no runner instances) and `nix/hosts/home-lab/secrets/glr-an.env` (encrypted secret with a GitLab runner authentication token, not yet wired into sops).
- Asked user for GitLab server URL and executor preference; user confirmed they had already set `CI_SERVER_URL=https://gitlab.com` and `CI_SERVER_TOKEN=glrt-...` in `glr-an.env` (verified via `sops -d`).
- Modified `nix/hosts/home-lab/modules/sops.nix` — added `sops.secrets.glr-an` (dotenv format, sourced from `../secrets/glr-an.env`, `restartUnits = ["gitlab-runner.service"]`).
- Modified `nix/system-modules/services/gitlab-runner.nix` — configured `services.gitlab-runner.services.an` with `executor = "docker"`, `dockerImage = "alpine:latest"`, `authenticationTokenConfigFile` pointing at the new sops secret, and `dockerVolumes = ["/var/run/docker.sock:/var/run/docker.sock"]` (mirrors the upstream NixOS module's own documented example for authentication-token-based docker runners).
- Verified via nixpkgs source (`nixos/modules/services/continuous-integration/gitlab-runner/runner.nix`) that `dockerImage`/`dockerVolumes`/`dockerDisableCache` are valid submodule options, and that the module auto-adds `podman.service` dependency + `podman` supplementary group when `virtualisation.podman.dockerSocket.enable = true` (already set in `nix/system-modules/features/podman.nix`), so no extra virtualisation wiring was needed.
- Ran `just check` (`nix flake check`) — passed (only a "dirty git tree" warning, as expected for uncommitted changes).

### Outcome

✅ GitLab Runner ("an") fully wired up with docker executor via existing podman docker-compat socket, authentication token secret registered through sops-nix, and `nix flake check` passes. Not yet committed — awaiting user review/deploy.
