# AI Work Log - 2026-04-21

## 2026-04-21T07:11:12+10:00

### Prompt

> For some reason I'm not able to deploy openclaw with nix.

### Model

Claude Opus 4.7 via Cursor

### Actions

- Reading openclaw module to diagnose deployment failure
- Ran `nix flake check` and `nix eval` — config evaluated cleanly; could not reproduce the deploy error locally
- Noted likely issues in `nix/home/modules/openclaw/openclaw.nix`: missing required `channels.telegram.*`, invalid model string `"ollama/llama3.21:b"`, unpinned `nix-openclaw` input, and a `permittedInsecurePackages` that may drift from upstream version
- User pivoted: remove all openclaw + home-manager references entirely
- Edited `flake.nix` — removed `home-manager` and `nix-openclaw` inputs, removed them from outputs fn args, removed `home-manager.nixosModules.home-manager` + inline `extraSpecialArgs` module, dropped `homeConfigurations.bose-game`
- Edited `nix/hosts/home-lab/configuration.nix` — removed the `home-manager = { ... users.bose-game = { imports = [...]; }; }` block and `nixpkgs.config.permittedInsecurePackages = ["openclaw-2026.4.11"]`
- Deleted `nix/home/` entirely (openclaw home-manager module, home-lab user file, home-specific sops module, `openclawGatewayToken` sops secret, documents dir)
- Edited `justfile` — updated `upp` comment example from `i=home-manager` to `i=nixpkgs`
- Ran `nix flake lock` — removed stale `home-manager`, `nix-openclaw`, and transitive `nix-steipete-tools` entries from `flake.lock`
- Ran `nix flake check` — all checks pass

### Outcome

✅ All openclaw/home-manager references removed; `nix flake check` passes cleanly

## 2026-04-21T07:43:31+10:00

### Prompt

> Setup hermes agent as per this guide — https://hermes-agent.nousresearch.com/docs/getting-started/nix-setup. Set it up as a NixOS container (systemd-nspawn) — https://wiki.nixos.org/wiki/NixOS_Containers

### Model

Claude Opus 4.7 via Cursor

### Actions

- Fetched the upstream Hermes Agent Nix guide; confirmed `github:NousResearch/hermes-agent` exports `nixosModules.default` with a `services.hermes-agent` option
- Decided on approach: run the upstream module inside a declarative NixOS `containers.<name>` (systemd-nspawn) rather than the module's own docker/podman container mode, per the user's explicit request
- User follow-up: they will configure secrets; agent wires the rest only
- Added `hermes-agent` flake input (`follows nixpkgs`) and passed `hermes-agent` through `specialArgs` for `nixosConfigurations.home-lab`
- Ran `nix flake lock` — lockfile now tracks `hermes-agent` and its transitive inputs
- Created `nix/system-modules/services/hermes-agent.nix` — `containers.hermes-agent` with host bind mounts for `${persistent_storage}/hermes-agent` and sops-decrypted env at `/run/secrets/hermes-env`; guest imports `hermes-agent.nixosModules.default`, enables `services.hermes-agent` (no Docker/Podman `container.enable` — that is a different feature from NixOS nspawn)
- Set `privateNetwork = false` on the nspawn container so outbound LLM API calls work without host NAT for `ve-*`; tmpfiles `0777` on persist dir to avoid UID skew on bind mounts
- Extended `nix/hosts/home-lab/modules/sops.nix` with `sops.secrets.hermes-env` (dotenv `../secrets/hermes.env`, `restartUnits` for `container@hermes-agent.service`) — user creates/encrypts `hermes.env`
- Edited `cog.toml` — scope `hermes-agent`; edited `.cspell.json` — word `hermes`
- Edited `nix/hosts/home-lab/secrets/README.md` — Hermes env file note
- Ran `nix flake check` — passed
- Ran `alejandra` on touched Nix files — clean

### Outcome

✅ Hermes Agent runs in a declarative NixOS (systemd-nspawn) container on home-lab; user adds encrypted `nix/hosts/home-lab/secrets/hermes.env` with provider keys

## 2026-04-21T07:59:51+10:00

### Prompt

> add hermes agent's container config too

### Model

Claude Opus 4.7 via Cursor

### Actions

- Edited `nix/system-modules/services/hermes-agent.nix` — in the **guest** config: enabled upstream `services.hermes-agent.container` (`enable`, `backend = "podman"`, `image = "ubuntu:24.04"`), enabled `virtualisation.podman` (mirroring homelab `features/podman.nix`), `virtualisation.docker.enable = lib.mkForce false` per Hermes docs for Podman
- On the **host** `containers.hermes-agent` entry: set `timeoutStartSec = "10min"` for first-time OCI image pull inside the guest
- Ran `nix flake check` — passed; ran `alejandra` on the module — clean

### Outcome

✅ Hermes OCI/container mode is enabled inside the NixOS nspawn guest alongside the existing bind mounts and service options
