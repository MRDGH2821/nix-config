# AI Work Log - 2026-06-21

## 2026-06-21Taudit+1000

### Prompt

> Audit nix-config for TECH DEBT & ARCHITECTURE and DX & TOOLING (playbook sections 5, 7). Return playbook-format findings only. No fixes.

### Model

Composer via Cursor

### Actions

- Read audit-playbook.md sections 5, 7, and Finding format
- Explored flake outputs, service/container modules, justfile, README, nix-cmds.sh, AGENTS.md, CI, pre-commit, treefmt
- Ran `nix flake show`, `nix flake check`, line-count and grep passes for duplication/dead code
- Compared README vs just/direnv; AGENTS.md vs .pre-commit-config.yaml; secrets README vs hermes modules

### Outcome

✅ Read-only audit complete; playbook-format findings returned to parent agent

## 17:40:06+1000

### Prompt

> Audit nix-config for CORRECTNESS/BUGS only (playbook section 1). Return playbook-format findings only.

### Model

Composer via Cursor

### Actions

- Read audit-playbook.md sections "## 1. Correctness / Bugs" and "## Finding format"
- Ran `just check`, `nix build` dry-run, and targeted `nix eval` on home-lab config (forgejo domain, nextcloud hostName, systemd mounts, peertube enable)
- Reviewed flake specialArgs/mylibFor wiring, service modules, container compose drift, sops secret declarations, rclone mounts, homepage-dashboard

### Outcome

✅ Correctness audit complete; playbook-format findings returned

## 16:45:44+1000

### Prompt

> fix this path import error

### Model

Composer via Cursor

### Actions

- Ran `just check` — failed with forbidden path `/nix/store/mylib/auto-import.nix` from `nix/home-modules/default.nix`
- Traced root cause: home modules moved from `nix/hosts/home-lab/home/` to `nix/home-modules/` but import kept `../../../mylib/` (valid at old depth, escapes store at new depth)
- Verified `desktop.nix` `../../../mylib/rclone-mounts.nix` is correct for `nix/hosts/home-lab/modules/`
- Fixed `nix/home-modules/default.nix` — changed import to `../mylib/auto-import.nix`
- Ran `just check` — all checks passed

### Outcome

✅ Fixed stale mylib import path after home-modules directory move; flake check passes

## 17:15:41+1000

### Prompt

> /improve-codebase-architecture

### Model

Composer via Cursor

### Actions

- Analyzed flake layout, module import graph, and repeated `mylib` relative-path patterns
- Added `nix/mylib/default.nix` — unified entry point composing auto-import, rclone-mounts, and domain-builder
- Wired `mylib`, `mylibPath`, and `mylibFor` through flake `specialArgs` (and home-manager `extraSpecialArgs`) to eliminate depth-sensitive relative imports
- Simplified 12 `default.nix` auto-import modules to use `mylib.autoImportModules` / `mylib.autoImportFolders` directly
- Updated jellyfin, navidrome, desktop, and homepage-dashboard to use `mylibFor` for context-dependent helpers
- Fixed AGENTS.md stale `nix/modules/` paths → `nix/system-modules/` and documented mylib access pattern
- Ran `just check` — all checks passed

### Outcome

✅ Centralized mylib via flake specialArgs; removed fragile relative imports and auto-import boilerplate across the module tree

## 17:44:52+1000

### Prompt

> Security audit of nix-config homelab flake (playbook section 2). Return playbook-format findings only. No fixes.

### Model

Composer via Cursor

### Actions

- Read audit-playbook.md sections 2 (Security) and Finding format
- Reviewed nix/hosts/home-lab/, nix/system-modules/services/, container-services/, vars/, sops config, and secrets layout
- Grepped for credentials, firewall, SSH, container host network, privileged mode, ACME/TLS, admin port exposure

### Outcome

✅ Security audit complete; playbook-format findings returned to parent agent

## 18:30:00+1000

### Prompt

> apply 004, 005

### Model

Composer via Cursor

### Actions

- Modified `nix/system-modules/services/forgejo.nix` — set `server.DOMAIN` to `git.${baseDomain}` to match `ROOT_URL`; reduced `log.LEVEL` from `Trace` to `Info`
- Modified `nix/system-modules/services/nextcloud.nix` — set `hostName` to `nc.${baseDomain}`; nginx vhost key follows via existing reference

### Outcome

✅ Plans 004 and 005 applied; `just check` passes
